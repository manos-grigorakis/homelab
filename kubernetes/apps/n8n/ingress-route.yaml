apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: n8n
  namespace: n8n
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`n8n.poolnode.net`)
      kind: Rule
      services:
        - name: n8n-main
          port: 5678
  tls:
    secretName: poolnode-wildcard-tls
