apiVersion: v1
kind: Service
metadata:
  name: nut-exporter
  namespace: monitoring
  labels:
    app: nut-exporter
spec:
  selector:
    app: nut-exporter
  ports:
    - name: metrics
      port: 9995
      targetPort: 9995
