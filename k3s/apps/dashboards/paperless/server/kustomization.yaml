apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paperless

resources:
  - data-pvc.yaml
  - media-pvc.yaml

  - vault-secrets.yaml
  - configmap.yaml
  - service.yaml

  - deployment.yaml
  - ingress-route.yaml
