apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: databases

resources:
  - service-account.yaml
  - secret-provider-class.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
